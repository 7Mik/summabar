{
  "name": "summabar",
  "version": "1.0.0",
  "description": "Streamlined client-only browser extension for summarizing YouTube videos and comments using external LLM providers",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "tubezero": "^1.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
